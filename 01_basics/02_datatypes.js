"use strict"; //Treats all JS code as newer Version.

//alert(6+6); // can't be used here like this, requires a different syntax to do this 
/*
ye alert browser me chalayenge to 6 ka popup show krega because there it is written in document , yahan things a different
*/

//Ecma and mdn(mozilla) are made for the standards of the Javascript.

//JS me bahut zyada Data types nhi hain.

let name="Kartike" //String data types prefer to use in " double quotes "

let age = 11 //integer data type

let isLoggedIn = false //boolean value -> true/false

/*
number = 2^53
bigint is used for very large number values
string = ""
null -> standalone value, is a type, it is a object, khaali, 0 is different and Null is also different, 0 can be a temperature Null can't.
undefined-> abhi value assign nhi hui hai
symbol-> talks about uniqueness
*/  
//typeof tells the type of the variable
console.log(typeof null) // object aata hai , fault of JS language
console.log(typeof undefined); // undefined has a different type->undefined


